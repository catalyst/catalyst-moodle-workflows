name: 'Plugin setup'
description: 'Run plugin setup'
inputs:
  php:
    description: 'PHP version to use'
    required: true
  node:
    description: 'Node.js version to use'
    required: false
    default: ''
  extra_php_extensions:
    description: 'List of additional php packages to install'
  extra_plugin_runners:
    description: 'Command to install dependencies'
  moodle_branch:
    description: 'Moodle branch to use (for patching)'
  database:
    description: 'Database type (e.g. pgsql, mariadb). Leave empty to skip installation.'
    required: false
    default: ''
runs:
  using: "composite"
  steps:
    - name: Set custom environment variables
      shell: bash
      run: |
        USER_IGNORE_PATHS="${{ inputs.ignore_paths }}"
        if [ -n "${USER_IGNORE_PATHS}" ]; then
          IGNORE_PATHS="${IGNORE_PATHS},${USER_IGNORE_PATHS}"
          echo "IGNORE_PATHS=${IGNORE_PATHS}" >> $GITHUB_ENV
        fi
    - name: Check out repository code
      uses: actions/checkout@v3
      with:
        path: plugin
        submodules: true
    - name: Install node ${{ inputs.node }}
      if: ${{ inputs.node != '' }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ inputs.node }}

    - name: Setup PHP ${{ inputs.php }}
      uses: shivammathur/setup-php@9e72090525849c5e82e596468b86eb55e9cc5401 # v2.32.0
      with:
        php-version: ${{ inputs.php }}
        ini-values: max_input_vars=5000
        extensions: pgsql, mysqli, zip, gd, xmlrpc, soap, ${{ inputs.extra_php_extensions }}
        coverage: none

    - name: Pull snapshot Docker image and extract artifacts
      shell: bash
      run: |
        BRANCH="${{ inputs.moodle_branch }}"
        # Use provided database for image name; fall back to mariadb for jobs with no database.
        DB="${{ inputs.database || 'mariadb' }}"
        IMAGE="ghcr.io/catalyst/snapshot-${BRANCH}-${DB}"
        IMAGE="${IMAGE//_/-}"
        IMAGE="${IMAGE,,}:latest"
        echo "Pulling snapshot image $IMAGE..."
        docker pull "$IMAGE"
        echo "SNAPSHOT_IMAGE=$IMAGE" >> "$GITHUB_ENV"

        CID=$(docker create "$IMAGE")
        docker cp "$CID:/m-ci.tar.gz" ./m-ci.tar.gz
        docker cp "$CID:/moodle.tar.gz" ./moodle.tar.gz
        docker rm "$CID"
        tar -C "$GITHUB_WORKSPACE" -xzf "$GITHUB_WORKSPACE/m-ci.tar.gz"
        tar -C "$GITHUB_WORKSPACE" -xzf "$GITHUB_WORKSPACE/moodle.tar.gz"
        mkdir -p "$GITHUB_WORKSPACE/moodledata"

    - name: Add moodle-plugin-ci binaries to PATH
      run: |
        START_TS=$(date +%s)
        echo "::group::Add moodle-plugin-ci binaries to PATH"

        # Add dirs to $PATH
        echo $(cd m-ci/bin; pwd) >> $GITHUB_PATH
        echo $(cd m-ci/vendor/bin; pwd) >> $GITHUB_PATH

        END_TS=$(date +%s)
        echo "Add moodle-plugin-ci binaries to PATH took $((END_TS - START_TS))s"
        echo "::endgroup::"
      shell: bash

    - name: Generate en_AU locale
      run: |
        START_TS=$(date +%s)
        echo "::group::Generate en_AU locale"

        # PHPUnit depends on en_AU.UTF-8 locale
        sudo locale-gen en_AU.UTF-8

        END_TS=$(date +%s)
        echo "Generate en_AU locale took $((END_TS - START_TS))s"
        echo "::endgroup::"
      shell: bash

    - name: Install dependencies
      if: ${{ inputs.extra_plugin_runners }}
      run: |
        START_TS=$(date +%s)
        # Install dependencies
        echo "::group::Install dependencies"

        ${{ inputs.extra_plugin_runners }}

        END_TS=$(date +%s)
        echo "Install dependencies took $((END_TS - START_TS))s"
        echo "::endgroup::"
      shell: bash

    - name: Apply plugin branch patch
      run: |
        START_TS=$(date +%s)
        echo "::group::Apply plugin branch patch"

        TARGET_REPO="$GITHUB_WORKSPACE/moodle"
        PATCH="$GITHUB_WORKSPACE/plugin/patch/${{ inputs.moodle_branch }}.diff"

        if [ -f "$PATCH" ]; then
          # moodle/ was extracted from a tarball (no .git); init a repo just for patch application.
          git config --global user.email "test@test.com"
          git config --global user.name "Test"
          git -C "$TARGET_REPO" init --quiet
          git -C "$TARGET_REPO" apply --whitespace=nowarn "$PATCH"
        else
          echo "No plugin branch patch found for ${{ inputs.moodle_branch }}"
        fi

        END_TS=$(date +%s)
        echo "Apply plugin branch patch took $((END_TS - START_TS))s"
        echo "::endgroup::"
      shell: bash

    - name: Install plugin into Moodle
      shell: bash
      run: |
        COMPONENT=$(php -r "
          define('MOODLE_INTERNAL', true);
          define('MATURITY_ALPHA', 50); define('MATURITY_BETA', 100);
          define('MATURITY_RC', 150); define('MATURITY_STABLE', 200);
          \$plugin = new stdClass();
          include 'plugin/version.php';
          echo \$plugin->component;
        ")
        PLUGIN_PATH=$(php -r "
          define('CLI_SCRIPT', true);
          define('ABORT_AFTER_CONFIG', true);
          require 'moodle/config.php';
          require_once \$CFG->dirroot . '/lib/classes/component.php';
          list(\$type, \$name) = core_component::normalize_component('$COMPONENT');
          echo core_component::get_plugin_directory(\$type, \$name);
        ")
        mkdir -p "$PLUGIN_PATH"
        cp -r plugin/. "$PLUGIN_PATH/"
        rm -rf "$PLUGIN_PATH/.git"

        echo "MOODLE_DIR=$GITHUB_WORKSPACE/moodle" >> "$GITHUB_ENV"
        echo "PLUGIN_DIR=$PLUGIN_PATH" >> "$GITHUB_ENV"

    - name: Create database and restore PHPUnit snapshot
      if: ${{ inputs.database != '' }}
      shell: bash
      env:
        DB: ${{ inputs.database }}
      run: |
        START_TS=$(date +%s)
        echo "::group::Create database and restore PHPUnit snapshot"

        # Extract phpunit snapshot zip from the image — fail if missing.
        CID=$(docker create "$SNAPSHOT_IMAGE")
        docker cp "$CID:/phpunit-snapshot.zip" ./phpunit-snapshot.zip
        docker rm "$CID"

        # Create the Moodle database.
        if [ "$DB" = "pgsql" ]; then
          psql -h 127.0.0.1 -U postgres -d postgres -c "DROP DATABASE IF EXISTS moodle;"
          psql -h 127.0.0.1 -U postgres -d postgres -c "CREATE DATABASE moodle;"
        else
          mysql -h 127.0.0.1 -u root -e "DROP DATABASE IF EXISTS moodle; CREATE DATABASE moodle DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"
        fi

        # Resolve phpunit_dataroot without a DB connection.
        PHPUNIT_DATAROOT=$(php -r "define('ABORT_AFTER_CONFIG', true); require 'moodle/config.php'; echo \$CFG->phpunit_dataroot;")
        mkdir -p "$PHPUNIT_DATAROOT"
        cp "$GITHUB_WORKSPACE/phpunit-snapshot.zip" "$PHPUNIT_DATAROOT/ci-phpunit.zip"

        # Detect phpunit util location (differs between classic and public/ layouts).
        if [ -f "$GITHUB_WORKSPACE/moodle/public/admin/tool/phpunit/cli/util.php" ]; then
          PHPUNIT_UTIL="$GITHUB_WORKSPACE/moodle/public/admin/tool/phpunit/cli/util.php"
        elif [ -f "$GITHUB_WORKSPACE/moodle/admin/tool/phpunit/cli/util.php" ]; then
          PHPUNIT_UTIL="$GITHUB_WORKSPACE/moodle/admin/tool/phpunit/cli/util.php"
        else
          echo "Could not locate phpunit util.php"
          exit 1
        fi

        php "$PHPUNIT_UTIL" --restore=ci-phpunit
        php "$PHPUNIT_UTIL" --upgrade

        END_TS=$(date +%s)
        echo "Create database and restore PHPUnit snapshot took $((END_TS - START_TS))s"
        echo "::endgroup::"


