name: composer-publish

on:
  workflow_call:
    secrets:
      COMPOSER_TRIGGER_URL:
        required: true
      COMPOSER_TRIGGER_TOKEN:
        required: true

jobs:
  publish_composer:
    name: Trigger GitLab pipeline
    runs-on: ubuntu-latest
    steps:
      - name: Check out plugin code
        uses: actions/checkout@v3
        with:
          fetch-depth: 1
          submodules: true

      - name: Trigger GitLab pipeline
        env:
          COMPOSER_TRIGGER_URL: ${{ secrets.COMPOSER_TRIGGER_URL }}
          COMPOSER_TRIGGER_TOKEN: ${{ secrets.COMPOSER_TRIGGER_TOKEN }}
        run: |
          set -euo pipefail
          curl --silent --show-error --fail \
            --request POST \
            --header "Content-Type: application/x-www-form-urlencoded" \
            --data-urlencode "token=${COMPOSER_TRIGGER_TOKEN}" \
            --data-urlencode "ref=main" \
            --data-urlencode "variables[source_repo]=git@github.com:${GITHUB_REPOSITORY}.git" \
            --data-urlencode "variables[source_commit]=${GITHUB_SHA}" \
            "${COMPOSER_TRIGGER_URL}"